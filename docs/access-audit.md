# Access audit — what the Notion tokens can / can't reach

> Generated by `scripts/audit-access.sh` + manual probes (re-run after changes).
> Last run: 2026-06-26, using **Dwayne's PAT** as the active token.

## Two tokens, two different credentials

`.env` holds two Notion tokens. `ntn` only reads the variable named
`NOTION_API_TOKEN`.

| Var in `.env` | Integration name | Type | Owner | Reaches |
| --- | --- | --- | --- | --- |
| `NOTION_API_TOKEN` (primary) | **Dwayne's PAT** | bot (PAT, owned by you) | `dwayne@agiventures.ca` | Ecosystem teamspace + 12 / 24 CRM DBs |
| `NOTION_API_TOKEN_LEGACY` | notion-labs | bot (internal integration) | — | 9 / 24 CRM DBs; **no** Ecosystem |

**Key distinction (confirmed by probing):**

- `notion-labs` is a plain internal integration. It only sees pages where the
  "notion-labs" connection was explicitly added, and it is **completely blocked
  from the Ecosystem teamspace** (that teamspace disallows integrations).
- `Dwayne's PAT` is owned by you and acts like your user identity. It
  **bypasses the Ecosystem teamspace integration block** — it can read the hub,
  subpages, and databases inside Ecosystem (confirmed: Events DB queried
  successfully). It also reaches 12 CRM databases, including Contacts,
  Companies, and Projects that `notion-labs` could not.
- The PAT is still blocked from 12 other CRM databases (Opportunities, Docs,
  Emails, Invoices, etc.). Because the PAT *does* reach the Ecosystem teamspace,
  those 12 blocks are **not** a teamspace-wide integration block — they're
  restrictions on those specific databases/areas (likely per-database sharing or
  a restricted area). They need a separate look.

## How integration access works in Notion

A plain integration (like `notion-labs`) can only see a page/database if both
are true:

1. The **teamspace** allows integrations (teamspace setting, owner-controlled).
2. The integration is added as a **connection** to that page (or a parent and
   cascaded).

A **personal access token** that acts as the user (like `Dwayne's PAT`) is not
stopped by the teamspace "block integrations" setting — it represents the user,
not an integration. It can still be blocked by per-page/per-database
restrictions that apply to the user's access.

## Ecosystem teamspace — confirmed accessible to the PAT

The Ecosystem teamspace blocks integrations, so `notion-labs` cannot reach any
of it. **`Dwayne's PAT` can.** Probed 2026-06-26:

| Probe | Result |
| --- | --- |
| `ntn pages get` Ecosystem hub (`2dbffe5c4f74800da0f2d30c424f8acc`) | ✅ returned full page |
| `ntn pages get` Contacts & CRM subpage | ✅ returned |
| `ntn pages get` Initiatives & Programs subpage | ✅ returned (incl. DB links) |
| `ntn datasources query` Events data source | ✅ returned rows (17 KB) |

Ecosystem hub: `https://app.notion.com/p/Ecosystem-2dbffe5c4f74800da0f2d30c424f8acc`

Ecosystem section subpages (all reachable by the PAT):

| Section | ID |
| --- | --- |
| Company | `38affe5c4f74819b9978d532184e94d4` |
| Community | `38affe5c4f74818b99fec2fdf386d42e` |
| Meetings | `38affe5c4f7481be99abf62d05c09785` |
| Initiatives & Programs | `38affe5c4f7481b29282c9dcc4eef642` |
| Contacts & CRM | `38affe5c4f7481c68897f24fc66cec8d` |
| Notes & Postmortems | `2dbffe5c4f7481c9adfbf31950549c31` |
| Archive | `38affe5c4f7481a3ab3fd45bb8ca9638` |

Ecosystem databases discovered (reachable by the PAT):

| Database | Database ID | Data source ID |
| --- | --- | --- |
| Events | `2dbffe5c4f74816a8e19d5410fb755c6` | `collection://2dbffe5c-4f74-812d-bbcc-000b2c05cc51` |
| Sponsorship Opportunities | `34691ce34f484c238de33e335b251245` | (resolve to confirm) |
| Inline DB (under Initiatives) | `2dbffe5c4f7481dc8efbe142adafab3f` | (resolve to confirm) |

> These Ecosystem DBs are **not** in the `where-to-find-data` skill (which maps
> the CRM/Applied AI area). To find more Ecosystem databases, fetch the relevant
> subpage with `ntn pages get <id>` and read the `<database …>` links.

## Audit: Dwayne's PAT (primary) — 12 shared / 12 no-access

Probed via `ntn datasources resolve <db-id>` with `NOTION_API_TOKEN` = PAT.

### ✅ Accessible (12)

| Database | ID |
| --- | --- |
| Contacts | `2dbffe5c4f74812f8711cba5079053a8` |
| Companies | `2dbffe5c4f7481d2815cde1959ba73a2` |
| Meetings | `2e1ffe5c4f7480eca457cd2147a987ba` |
| Pitches | `2dbffe5c4f7481d5b902dc705ae4fb75` |
| Deliverables | `2f4ffe5c4f748080b246f89fa339e991` |
| Public Workshops | `2dbffe5c4f748165bf3adf7fa0b2063b` |
| Revenue Tasks (FDE) | `847aa7ed358d4873a3c1f727b16a8731` |
| Sprints | `67e3be48e3b949c49a49e815d4b74861` |
| Product SKUs | `2fcffe5c4f748054b992fd61c14c2b9b` |
| Goals | `9d114b6cd0e84bd3910c45a0b16fb460` |
| Ideas | `2dbffe5c4f7481398ee0f5d86fc3a2cb` |
| Projects | `a98ffe5c4f748248b51a81af4141c0b1` |

### ❌ Not accessible (12) — per-database restrictions, not a teamspace block

| Database | ID |
| --- | --- |
| Opportunities | `2dbffe5c4f7481b8a0c8ceb7a6d0f30e` |
| Company Info (AGIVC) | `44933df344334963b9842058e9a87be2` |
| Lessons Learned | `359ffe5c4f7480709eedc6307fe9971e` |
| Docs | `2dbffe5c4f74812ca415e1a6f952edeb` |
| Emails | `218fa0876eec414ea00b0c31145a3a87` |
| Invoices | `bc09dc1adbfa40279ff18beff54bd720` |
| Contractors | `2dbffe5c4f748140990fca536ce6cd57` |
| Contractor Contracts | `6118fdc71aae4d3184a32a306239baaa` |
| Applied AI Hours | `2f4ffe5c4f74807393e2ebc90348a649` |
| Content Calendar | `2deffe5c4f74803c9bf1e7403e61b40c` |
| Jobs Description | `2feffe5c4f74803fb0a9ee462f3855fb` |
| Candidates | `328ffe5c4f74806094c9cb8cd62ba812` |

These are the deal/billing/HR area. Since the PAT reaches the Ecosystem
teamspace, these 12 blocks are per-database/area restrictions, not a
teamspace-wide integration block. To unlock them, check each database's share
settings in Notion (or share a parent page in that area and cascade), then
re-audit. If they remain blocked even after sharing, the user account itself
may have limited access there.

### Why the 12 are blocked (investigation, 2026-06-26)

Confirmed via the Notion MCP (which runs as a third integration, `1f8d872b-…`,
acting as Dwayne):

- MCP fetches **Ecosystem** and **Contacts** successfully, but **404s on
  Opportunities and Invoices** — the same `object_not_found` as the PAT. So all
  three API integrations (PAT, `notion-labs`, MCP plugin) are blocked from the
  same 12 databases. It is **not** PAT-specific.
- `notion-get-teams` shows Dwayne is a member of all 3 teamspaces —
  `Dwayne's Test Teamspace` (owner), `Ecosystem` (member), `Revenue Org`
  (member). `otherTeams` is empty, so no teamspace is off-limits by membership.
- Opportunities definitely exists: the accessible Contacts DB has an
  `Opportunities` relation pointing at data source
  `collection://2dbffe5c-4f74-81cb-9b53-000b23acf6c0`.

**Diagnosis:** the 12 blocked databases are individually **restricted** pages
(common for sensitive deal/billing/HR DBs like Opportunities, Invoices,
Contractors, Candidates). Restricted pages do not auto-share with integrations
the way open teamspace pages do — each integration must be added to the page's
access list explicitly. Ecosystem, by contrast, is member-accessible, so the
user-acting PAT/MCP reach it via membership (while the pure-bot `notion-labs`
is blocked by the teamspace's integration setting).

### How to unblock the 12 (in Notion)

> **Update (2026-06-26):** the boss flagged these 12 as **not canonical**
> (legacy/secondary). Unblocking them is therefore **optional / low priority**.
> The 12 accessible DBs + Ecosystem are the active set; `docs/data-map.md` now
> reflects canonical status. Keep the steps below for if/when access is needed.

For each blocked database (or a parent page that contains them, cascaded):

1. Open the database in Notion.
2. **Share** menu (or `•••` → **Connections** → **Add connections**).
3. Add **`Dwayne's PAT`** (for CLI/PAT access). Add the **notion-workspace**
   integration too if you want MCP access there.
4. Confirm, then re-run `scripts/audit-access.sh`.

If a database is in a teamspace whose settings you can't change (you're a
*member*, not owner, of Ecosystem and Revenue Org) and the integration can't be
added, ask a teamspace owner to either enable integrations or add the
integration to those pages for you.

## Audit: notion-labs (legacy) — 9 shared / 15 no-access

For comparison (run by exporting `NOTION_API_TOKEN_LEGACY` as `NOTION_API_TOKEN`).

- **Accessible (9):** Meetings, Pitches, Deliverables, Public Workshops, Revenue
  Tasks (FDE), Sprints, Product SKUs, Goals, Ideas.
- **Not accessible (15):** the 12 above + Contacts, Companies, Projects.
- **Ecosystem:** fully blocked (teamspace disallows integrations).

The PAT strictly dominates `notion-labs` for the CRM, and is the **only** token
that can reach Ecosystem.

## Re-running the audit

```bash
scripts/audit-access.sh            # human-readable table (uses NOTION_API_TOKEN)
scripts/audit-access.sh --json     # machine-readable

# Audit the legacy integration instead:
NOTION_API_TOKEN="$NOTION_API_TOKEN_LEGACY" scripts/audit-access.sh

# Probe a specific page/database with the PAT:
set -a; source .env; set +a
ntn pages get <page-id>
ntn datasources query <data-source-id> --limit 5 --json
```
